export const metadata = { title: "Analytics — urlx" };

import OverviewClient from "@/components/dashboard/analytics/OverviewClient";

export default function AnalyticsPage() {
  return <OverviewClient />;
}
